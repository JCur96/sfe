# preppData <- function() {
#   path <- readline(prompt = 'If wrangling IUCN data please enter path,
#                    if not please enter NULL: ')
#   pathOrName <- readline(prompt = 'please supply path to IUCN data OR
#                          supply a df name: ')
#   # work out if its a path or a DF name here (might be beyond R's scope)
#   if (pathOrName = path) {
#     # do IUCN setup
#   }
#   else{
#     # do NHM setup
#   }
# }
